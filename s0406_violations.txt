py2cpp/util/deque.py:18:5: [S0406] 类 `DequeIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/deque.py:54:5: [S0406] 类 `deque` 的 `__init__`请用 `类体 `_head: T = …` 字段默认`，勿用 ``__init__` 内 `self._head = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_head: int = 0` 且 `__init__` 不写 `self._head = 0`
py2cpp/util/deque.py:55:5: [S0406] 类 `deque` 的 `__init__`请用 `类体 `_tail: T = …` 字段默认`，勿用 ``__init__` 内 `self._tail = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_tail: int = 0` 且 `__init__` 不写 `self._tail = 0`
py2cpp/util/deque.py:56:5: [S0406] 类 `deque` 的 `__init__`请用 `类体 `_length: T = …` 字段默认`，勿用 ``__init__` 内 `self._length = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_length: int = 0` 且 `__init__` 不写 `self._length = 0`
py2cpp/util/dict.py:240:5: [S0406] 类 `FrozenDictKeyIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:252:5: [S0406] 类 `FrozenDictValuesIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:258:5: [S0406] 类 `FrozenDictItemsIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:305:5: [S0406] 类 `frozendict` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/dict.py:306:5: [S0406] 类 `frozendict` 的 `__init__`请用 `类体 `_size: T = …` 字段默认`，勿用 ``__init__` 内 `self._size = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_size: int = 0` 且 `__init__` 不写 `self._size = 0`
py2cpp/util/dict.py:372:5: [S0406] 类 `DictKeyIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:384:5: [S0406] 类 `DictValuesIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:390:5: [S0406] 类 `DictItemsIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/dict.py:444:5: [S0406] 类 `dict` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/dict.py:445:5: [S0406] 类 `dict` 的 `__init__`请用 `类体 `_size: T = …` 字段默认`，勿用 ``__init__` 内 `self._size = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_size: int = 0` 且 `__init__` 不写 `self._size = 0`
py2cpp/util/list.py:218:5: [S0406] 类 `ListIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/list.py:240:5: [S0406] 类 `FrozenListIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/util/list.py:251:5: [S0406] 类 `list` 的 `__init__`请用 `类体 `_length: T = …` 字段默认`，勿用 ``__init__` 内 `self._length = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_length: int = 0` 且 `__init__` 不写 `self._length = 0`
py2cpp/util/list.py:252:5: [S0406] 类 `list` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/list.py:630:5: [S0406] 类 `frozenlist` 的 `__init__`请用 `类体 `_length: T = …` 字段默认`，勿用 ``__init__` 内 `self._length = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_length: int = 0` 且 `__init__` 不写 `self._length = 0`
py2cpp/util/list.py:631:5: [S0406] 类 `frozenlist` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/misc.py:254:5: [S0406] 类 `CounterElementsIterator` 的 `__init__`请用 `类体 `_keyI: T = …` 字段默认`，勿用 ``__init__` 内 `self._keyI = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_keyI: int = 0` 且 `__init__` 不写 `self._keyI = 0`
py2cpp/util/pool.py:45:5: [S0406] 类 `Pool` 的 `__init__`请用 `类体 `_live: T = …` 字段默认`，勿用 ``__init__` 内 `self._live = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_live: int = 0` 且 `__init__` 不写 `self._live = 0`
py2cpp/util/range.py:44:5: [S0406] 类 `range` 的 `__init__`请用 `类体 `_start: T = …` 字段默认`，勿用 ``__init__` 内 `self._start = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_start: int = 0` 且 `__init__` 不写 `self._start = 0`
py2cpp/util/range.py:46:5: [S0406] 类 `range` 的 `__init__`请用 `类体 `_step: T = …` 字段默认`，勿用 ``__init__` 内 `self._step = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_step: int = 0` 且 `__init__` 不写 `self._step = 0`
py2cpp/util/set.py:238:5: [S0406] 类 `SetIterator` 的 `__init__`请用 `类体 `_bucket: T = …` 字段默认`，勿用 ``__init__` 内 `self._bucket = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_bucket: int = 0` 且 `__init__` 不写 `self._bucket = 0`
py2cpp/util/set.py:239:5: [S0406] 类 `SetIterator` 的 `__init__`请用 `类体 `_skip: T = …` 字段默认`，勿用 ``__init__` 内 `self._skip = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_skip: int = 0` 且 `__init__` 不写 `self._skip = 0`
py2cpp/util/set.py:250:5: [S0406] 类 `set` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/set.py:251:5: [S0406] 类 `set` 的 `__init__`请用 `类体 `_size: T = …` 字段默认`，勿用 ``__init__` 内 `self._size = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_size: int = 0` 且 `__init__` 不写 `self._size = 0`
py2cpp/util/set.py:424:5: [S0406] 类 `FrozenSetIterator` 的 `__init__`请用 `类体 `_bucket: T = …` 字段默认`，勿用 ``__init__` 内 `self._bucket = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_bucket: int = 0` 且 `__init__` 不写 `self._bucket = 0`
py2cpp/util/set.py:425:5: [S0406] 类 `FrozenSetIterator` 的 `__init__`请用 `类体 `_skip: T = …` 字段默认`，勿用 ``__init__` 内 `self._skip = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_skip: int = 0` 且 `__init__` 不写 `self._skip = 0`
py2cpp/util/set.py:437:5: [S0406] 类 `frozenset` 的 `__init__`请用 `类体 `_capacity: T = …` 字段默认`，勿用 ``__init__` 内 `self._capacity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_capacity: int = 0` 且 `__init__` 不写 `self._capacity = 0`
py2cpp/util/set.py:438:5: [S0406] 类 `frozenset` 的 `__init__`请用 `类体 `_size: T = …` 字段默认`，勿用 ``__init__` 内 `self._size = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_size: int = 0` 且 `__init__` 不写 `self._size = 0`
py2cpp/util/span.py:16:5: [S0406] 类 `span` 的 `__init__`请用 `类体 `_ptr: T = …` 字段默认`，勿用 ``__init__` 内 `self._ptr = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_ptr: int = 0` 且 `__init__` 不写 `self._ptr = 0`
py2cpp/util/span.py:17:5: [S0406] 类 `span` 的 `__init__`请用 `类体 `_length: T = …` 字段默认`，勿用 ``__init__` 内 `self._length = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_length: int = 0` 且 `__init__` 不写 `self._length = 0`
py2cpp/util/span.py:18:5: [S0406] 类 `span` 的 `__init__`请用 `类体 `_step: T = …` 字段默认`，勿用 ``__init__` 内 `self._step = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_step: int = 0` 且 `__init__` 不写 `self._step = 0`
py2cpp/text/str.py:22:5: [S0406] 类 `StrIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/text/str.py:268:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hash: T = …` 字段默认`，勿用 ``__init__` 内 `self._hash = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hash: int = 0` 且 `__init__` 不写 `self._hash = 0`
py2cpp/text/str.py:281:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hash: T = …` 字段默认`，勿用 ``__init__` 内 `self._hash = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hash: int = 0` 且 `__init__` 不写 `self._hash = 0`
py2cpp/text/str.py:287:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hash: T = …` 字段默认`，勿用 ``__init__` 内 `self._hash = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hash: int = 0` 且 `__init__` 不写 `self._hash = 0`
py2cpp/text/str.py:288:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hashOk: T = …` 字段默认`，勿用 ``__init__` 内 `self._hashOk = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hashOk: int = 0` 且 `__init__` 不写 `self._hashOk = 0`
py2cpp/text/str.py:297:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hash: T = …` 字段默认`，勿用 ``__init__` 内 `self._hash = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hash: int = 0` 且 `__init__` 不写 `self._hash = 0`
py2cpp/text/str.py:298:5: [S0406] 类 `str` 的 `__init__`请用 `类体 `_hashOk: T = …` 字段默认`，勿用 ``__init__` 内 `self._hashOk = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hashOk: int = 0` 且 `__init__` 不写 `self._hashOk = 0`
py2cpp/io/__init__.py:58:5: [S0406] 类 `StringIO` 的 `__init__`请用 `类体 `_pos: T = …` 字段默认`，勿用 ``__init__` 内 `self._pos = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_pos: int = 0` 且 `__init__` 不写 `self._pos = 0`
py2cpp/io/__init__.py:59:5: [S0406] 类 `StringIO` 的 `__init__`请用 `类体 `_closed: T = …` 字段默认`，勿用 ``__init__` 内 `self._closed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_closed: int = 0` 且 `__init__` 不写 `self._closed = 0`
py2cpp/io/__init__.py:261:5: [S0406] 类 `TextIO` 的 `__init__`请用 `类体 `_owns: T = …` 字段默认`，勿用 ``__init__` 内 `self._owns = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_owns: int = 0` 且 `__init__` 不写 `self._owns = 0`
py2cpp/io/__init__.py:262:5: [S0406] 类 `TextIO` 的 `__init__`请用 `类体 `_closed: T = …` 字段默认`，勿用 ``__init__` 内 `self._closed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_closed: int = 0` 且 `__init__` 不写 `self._closed = 0`
py2cpp/numeric/decimal.py:35:5: [S0406] 类 `Context` 的 `__init__`请用 `类体 `Emin: T = …` 字段默认`，勿用 ``__init__` 内 `self.Emin = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `Emin: int = 0` 且 `__init__` 不写 `self.Emin = 0`
py2cpp/numeric/decimal.py:36:5: [S0406] 类 `Context` 的 `__init__`请用 `类体 `Emax: T = …` 字段默认`，勿用 ``__init__` 内 `self.Emax = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `Emax: int = 0` 且 `__init__` 不写 `self.Emax = 0`
py2cpp/numeric/decimal.py:37:5: [S0406] 类 `Context` 的 `__init__`请用 `类体 `traps: T = …` 字段默认`，勿用 ``__init__` 内 `self.traps = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `traps: int = 0` 且 `__init__` 不写 `self.traps = 0`
py2cpp/numeric/decimal.py:38:5: [S0406] 类 `Context` 的 `__init__`请用 `类体 `flags: T = …` 字段默认`，勿用 ``__init__` 内 `self.flags = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `flags: int = 0` 且 `__init__` 不写 `self.flags = 0`
py2cpp/numeric/decimal.py:83:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_sign: T = …` 字段默认`，勿用 ``__init__` 内 `self._sign = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_sign: int = 0` 且 `__init__` 不写 `self._sign = 0`
py2cpp/numeric/decimal.py:85:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_exp: T = …` 字段默认`，勿用 ``__init__` 内 `self._exp = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_exp: int = 0` 且 `__init__` 不写 `self._exp = 0`
py2cpp/numeric/decimal.py:86:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_special: T = …` 字段默认`，勿用 ``__init__` 内 `self._special = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_special: int = 0` 且 `__init__` 不写 `self._special = 0`
py2cpp/numeric/decimal.py:91:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_sign: T = …` 字段默认`，勿用 ``__init__` 内 `self._sign = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_sign: int = 0` 且 `__init__` 不写 `self._sign = 0`
py2cpp/numeric/decimal.py:93:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_exp: T = …` 字段默认`，勿用 ``__init__` 内 `self._exp = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_exp: int = 0` 且 `__init__` 不写 `self._exp = 0`
py2cpp/numeric/decimal.py:94:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_special: T = …` 字段默认`，勿用 ``__init__` 内 `self._special = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_special: int = 0` 且 `__init__` 不写 `self._special = 0`
py2cpp/numeric/decimal.py:100:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_sign: T = …` 字段默认`，勿用 ``__init__` 内 `self._sign = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_sign: int = 0` 且 `__init__` 不写 `self._sign = 0`
py2cpp/numeric/decimal.py:102:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_exp: T = …` 字段默认`，勿用 ``__init__` 内 `self._exp = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_exp: int = 0` 且 `__init__` 不写 `self._exp = 0`
py2cpp/numeric/decimal.py:103:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_special: T = …` 字段默认`，勿用 ``__init__` 内 `self._special = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_special: int = 0` 且 `__init__` 不写 `self._special = 0`
py2cpp/numeric/decimal.py:109:5: [S0406] 类 `Decimal` 的 `__init__`请用 `类体 `_exp: T = …` 字段默认`，勿用 ``__init__` 内 `self._exp = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_exp: int = 0` 且 `__init__` 不写 `self._exp = 0`
py2cpp/numeric/long.py:21:5: [S0406] 类 `long` 的 `__init__`请用 `类体 `_neg: T = …` 字段默认`，勿用 ``__init__` 内 `self._neg = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_neg: int = 0` 且 `__init__` 不写 `self._neg = 0`
py2cpp/numeric/long.py:23:5: [S0406] 类 `long` 的 `__init__`请用 `类体 `_hash: T = …` 字段默认`，勿用 ``__init__` 内 `self._hash = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hash: int = 0` 且 `__init__` 不写 `self._hash = 0`
py2cpp/numeric/long.py:24:5: [S0406] 类 `long` 的 `__init__`请用 `类体 `_hashOk: T = …` 字段默认`，勿用 ``__init__` 内 `self._hashOk = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hashOk: int = 0` 且 `__init__` 不写 `self._hashOk = 0`
py2cpp/alg/ac_auto.py:31:5: [S0406] 类 `ACAuto` 的 `__init__`请用 `类体 `_count: T = …` 字段默认`，勿用 ``__init__` 内 `self._count = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_count: int = 0` 且 `__init__` 不写 `self._count = 0`
py2cpp/alg/ac_auto.py:32:5: [S0406] 类 `ACAuto` 的 `__init__`请用 `类体 `_flushed: T = …` 字段默认`，勿用 ``__init__` 内 `self._flushed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_flushed: int = 0` 且 `__init__` 不写 `self._flushed = 0`
py2cpp/alg/chunk_deque.py:15:5: [S0406] 类 `_ChunkNodeUnsafe` 的 `__init__`请用 `类体 `prev: T = …` 字段默认`，勿用 ``__init__` 内 `self.prev = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `prev: int = 0` 且 `__init__` 不写 `self.prev = 0`
py2cpp/alg/chunk_deque.py:16:5: [S0406] 类 `_ChunkNodeUnsafe` 的 `__init__`请用 `类体 `next: T = …` 字段默认`，勿用 ``__init__` 内 `self.next = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `next: int = 0` 且 `__init__` 不写 `self.next = 0`
py2cpp/alg/chunk_deque.py:22:5: [S0406] 类 `ChunkDequeIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/alg/chunk_deque.py:60:5: [S0406] 类 `ChunkDeque` 的 `__init__`请用 `类体 `_head: T = …` 字段默认`，勿用 ``__init__` 内 `self._head = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_head: int = 0` 且 `__init__` 不写 `self._head = 0`
py2cpp/alg/chunk_deque.py:61:5: [S0406] 类 `ChunkDeque` 的 `__init__`请用 `类体 `_tail: T = …` 字段默认`，勿用 ``__init__` 内 `self._tail = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_tail: int = 0` 且 `__init__` 不写 `self._tail = 0`
py2cpp/alg/chunk_deque.py:62:5: [S0406] 类 `ChunkDeque` 的 `__init__`请用 `类体 `_len: T = …` 字段默认`，勿用 ``__init__` 内 `self._len = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_len: int = 0` 且 `__init__` 不写 `self._len = 0`
py2cpp/alg/seg_tree.py:26:5: [S0406] 类 `SegTree` 的 `__init__`请用 `类体 `_size: T = …` 字段默认`，勿用 ``__init__` 内 `self._size = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_size: int = 0` 且 `__init__` 不写 `self._size = 0`
py2cpp/alg/trie.py:30:5: [S0406] 类 `Trie` 的 `__init__`请用 `类体 `_count: T = …` 字段默认`，勿用 ``__init__` 内 `self._count = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_count: int = 0` 且 `__init__` 不写 `self._count = 0`
py2cpp/design/ecs.py:37:5: [S0406] 类 `ECSComponentTableIterator` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/design/ecs.py:61:5: [S0406] 类 `ECSComponentTableQuery` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/serde/yaml.py:202:5: [S0406] 类 `_YamlParser` 的 `__init__`请用 `类体 `pos: T = …` 字段默认`，勿用 ``__init__` 内 `self.pos = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `pos: int = 0` 且 `__init__` 不写 `self.pos = 0`
py2cpp/serde/pyml.py:230:5: [S0406] 类 `_PymlExpander` 的 `__init__`请用 `类体 `returned: T = …` 字段默认`，勿用 ``__init__` 内 `self.returned = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `returned: int = 0` 且 `__init__` 不写 `self.returned = 0`
py2cpp/serde/pyml.py:232:5: [S0406] 类 `_PymlExpander` 的 `__init__`请用 `类体 `runKind: T = …` 字段默认`，勿用 ``__init__` 内 `self.runKind = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `runKind: int = 0` 且 `__init__` 不写 `self.runKind = 0`
py2cpp/concur/process.py:128:5: [S0406] 类 `Process` 的 `__init__`请用 `类体 `_daemon: T = …` 字段默认`，勿用 ``__init__` 内 `self._daemon = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_daemon: int = 0` 且 `__init__` 不写 `self._daemon = 0`
py2cpp/concur/task.py:98:5: [S0406] 类 `_CoroSlot` 的 `__init__`请用 `类体 `_useSend: T = …` 字段默认`，勿用 ``__init__` 内 `self._useSend = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_useSend: int = 0` 且 `__init__` 不写 `self._useSend = 0`
py2cpp/concur/task.py:297:5: [S0406] 类 `Scheduler` 的 `__init__`请用 `类体 `periodCount: T = …` 字段默认`，勿用 ``__init__` 内 `self.periodCount = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `periodCount: int = 0` 且 `__init__` 不写 `self.periodCount = 0`
py2cpp/concur/thread.py:477:5: [S0406] 类 `Barrier` 的 `__init__`请用 `类体 `_hasAction: T = …` 字段默认`，勿用 ``__init__` 内 `self._hasAction = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hasAction: int = 0` 且 `__init__` 不写 `self._hasAction = 0`
py2cpp/concur/thread.py:491:5: [S0406] 类 `Barrier` 的 `__init__`请用 `类体 `_hasAction: T = …` 字段默认`，勿用 ``__init__` 内 `self._hasAction = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hasAction: int = 0` 且 `__init__` 不写 `self._hasAction = 0`
py2cpp/concur/thread.py:503:5: [S0406] 类 `Barrier` 的 `__init__`请用 `类体 `_hasAction: T = …` 字段默认`，勿用 ``__init__` 内 `self._hasAction = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hasAction: int = 0` 且 `__init__` 不写 `self._hasAction = 0`
py2cpp/concur/thread.py:517:5: [S0406] 类 `Barrier` 的 `__init__`请用 `类体 `_hasAction: T = …` 字段默认`，勿用 ``__init__` 内 `self._hasAction = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_hasAction: int = 0` 且 `__init__` 不写 `self._hasAction = 0`
py2cpp/concur/thread.py:890:5: [S0406] 类 `Thread` 的 `__init__`请用 `类体 `_daemon: T = …` 字段默认`，勿用 ``__init__` 内 `self._daemon = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_daemon: int = 0` 且 `__init__` 不写 `self._daemon = 0`
py2cpp/console/render.py:143:5: [S0406] 类 `Progress` 的 `__init__`请用 `类体 `_active: T = …` 字段默认`，勿用 ``__init__` 内 `self._active = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_active: int = 0` 且 `__init__` 不写 `self._active = 0`
py2cpp/console/render.py:223:5: [S0406] 类 `Spinner` 的 `__init__`请用 `类体 `_index: T = …` 字段默认`，勿用 ``__init__` 内 `self._index = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_index: int = 0` 且 `__init__` 不写 `self._index = 0`
py2cpp/web/http.py:618:5: [S0406] 类 `_ClientStreamResponseState` 的 `__init__`请用 `类体 `status: T = …` 字段默认`，勿用 ``__init__` 内 `self.status = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `status: int = 0` 且 `__init__` 不写 `self.status = 0`
py2cpp/web/http.py:621:5: [S0406] 类 `_ClientStreamResponseState` 的 `__init__`请用 `类体 `_chunked: T = …` 字段默认`，勿用 ``__init__` 内 `self._chunked = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_chunked: int = 0` 且 `__init__` 不写 `self._chunked = 0`
py2cpp/web/http.py:622:5: [S0406] 类 `_ClientStreamResponseState` 的 `__init__`请用 `类体 `_closed: T = …` 字段默认`，勿用 ``__init__` 内 `self._closed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_closed: int = 0` 且 `__init__` 不写 `self._closed = 0`
py2cpp/web/http.py:623:5: [S0406] 类 `_ClientStreamResponseState` 的 `__init__`请用 `类体 `_done: T = …` 字段默认`，勿用 ``__init__` 内 `self._done = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_done: int = 0` 且 `__init__` 不写 `self._done = 0`
py2cpp/web/http.py:625:5: [S0406] 类 `_ClientStreamResponseState` 的 `__init__`请用 `类体 `_chunkPos: T = …` 字段默认`，勿用 ``__init__` 内 `self._chunkPos = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_chunkPos: int = 0` 且 `__init__` 不写 `self._chunkPos = 0`
py2cpp/web/http.py:731:5: [S0406] 类 `AsyncClientStreamResponse` 的 `__init__`请用 `类体 `status: T = …` 字段默认`，勿用 ``__init__` 内 `self.status = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `status: int = 0` 且 `__init__` 不写 `self.status = 0`
py2cpp/web/http.py:735:5: [S0406] 类 `AsyncClientStreamResponse` 的 `__init__`请用 `类体 `_chunked: T = …` 字段默认`，勿用 ``__init__` 内 `self._chunked = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_chunked: int = 0` 且 `__init__` 不写 `self._chunked = 0`
py2cpp/web/http.py:736:5: [S0406] 类 `AsyncClientStreamResponse` 的 `__init__`请用 `类体 `_closed: T = …` 字段默认`，勿用 ``__init__` 内 `self._closed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_closed: int = 0` 且 `__init__` 不写 `self._closed = 0`
py2cpp/web/http.py:737:5: [S0406] 类 `AsyncClientStreamResponse` 的 `__init__`请用 `类体 `_done: T = …` 字段默认`，勿用 ``__init__` 内 `self._done = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_done: int = 0` 且 `__init__` 不写 `self._done = 0`
py2cpp/web/http.py:739:5: [S0406] 类 `AsyncClientStreamResponse` 的 `__init__`请用 `类体 `_chunkPos: T = …` 字段默认`，勿用 ``__init__` 内 `self._chunkPos = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_chunkPos: int = 0` 且 `__init__` 不写 `self._chunkPos = 0`
py2cpp/test/unittest.py:30:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `testsRun: T = …` 字段默认`，勿用 ``__init__` 内 `self.testsRun = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `testsRun: int = 0` 且 `__init__` 不写 `self.testsRun = 0`
py2cpp/test/unittest.py:31:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `testsFailed: T = …` 字段默认`，勿用 ``__init__` 内 `self.testsFailed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `testsFailed: int = 0` 且 `__init__` 不写 `self.testsFailed = 0`
py2cpp/test/unittest.py:32:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `assertionsFailed: T = …` 字段默认`，勿用 ``__init__` 内 `self.assertionsFailed = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `assertionsFailed: int = 0` 且 `__init__` 不写 `self.assertionsFailed = 0`
py2cpp/test/unittest.py:36:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `_currentTag: T = …` 字段默认`，勿用 ``__init__` 内 `self._currentTag = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_currentTag: int = 0` 且 `__init__` 不写 `self._currentTag = 0`
py2cpp/test/unittest.py:37:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `_caseFailuresBefore: T = …` 字段默认`，勿用 ``__init__` 内 `self._caseFailuresBefore = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_caseFailuresBefore: int = 0` 且 `__init__` 不写 `self._caseFailuresBefore = 0`
py2cpp/test/unittest.py:38:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `verbosity: T = …` 字段默认`，勿用 ``__init__` 内 `self.verbosity = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `verbosity: int = 0` 且 `__init__` 不写 `self.verbosity = 0`
py2cpp/test/unittest.py:39:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `caseTotal: T = …` 字段默认`，勿用 ``__init__` 内 `self.caseTotal = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `caseTotal: int = 0` 且 `__init__` 不写 `self.caseTotal = 0`
py2cpp/test/unittest.py:40:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `_currentName: T = …` 字段默认`，勿用 ``__init__` 内 `self._currentName = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_currentName: int = 0` 且 `__init__` 不写 `self._currentName = 0`
py2cpp/test/unittest.py:41:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `_caseStart: T = …` 字段默认`，勿用 ``__init__` 内 `self._caseStart = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `_caseStart: int = 0` 且 `__init__` 不写 `self._caseStart = 0`
py2cpp/test/unittest.py:43:5: [S0406] 类 `TestResult` 的 `__init__`请用 `类体 `totalSeconds: T = …` 字段默认`，勿用 ``__init__` 内 `self.totalSeconds = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `totalSeconds: int = 0` 且 `__init__` 不写 `self.totalSeconds = 0`
py2cpp/test/unittest.py:117:5: [S0406] 类 `TestCase` 的 `__init__`请用 `类体 `failures: T = …` 字段默认`，勿用 ``__init__` 内 `self.failures = 字面量``（字面量/常量初值应生成 C++ 成员默认，初始化方法内再赋值为冗余）；例如 `failures: int = 0` 且 `__init__` 不写 `self.failures = 0`
